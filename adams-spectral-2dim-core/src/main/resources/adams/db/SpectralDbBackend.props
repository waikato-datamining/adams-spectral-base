# Defines what class provides the spectral DB backend interface implementations.

# Class name (+ options) that provides the backend
# For example: adams.db.mysql.SpectralDbBackend
# leave empty for internal autodetect
Backend=
