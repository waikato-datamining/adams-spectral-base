# Default named setups can be stored in here.
#
# Format:
#   <key>=<classname + options>
#
# Note:
# These setups are also used in the JUnit tests. Whenever modifying the setups
# here, make sure that all the tests still succeed.

#
# Author: FracPete (fracpete at waikato dot ac dot nz)
# Version: $Revision: 2187 $

instance_cleaner_passthrough=adams.data.cleaner.instance.PassThrough
spectrum_cleaner_passthrough=adams.data.cleaner.spectrum.PassThrough
instancegenerator_simple=adams.data.instances.SimpleInstanceGenerator
